services:
  - type: web
    name: my-fastapi-app
    runtime: python
    envVars:
      - key: PORT
        value: 8000
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
